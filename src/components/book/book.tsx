import React from "react"
import {table} from "../../styles/book.module.css"

const Book = () => {
  return (
    <section className={table}>
        <div>Book Mode</div>
    </section>
  )
}

export default Book